<h1>Followers:</h1>

<br>
<% if @subscriptions.present? %>
	<% @subscriptions.each do |subscription| %>
	  <li><%= link_to subscription.subscriber.username, user_url(subscription.subscriber_id) %> </li>
	<% end %>
<% else %>
	No followers yet.
<% end %>
<br>
<%= link_to 'Back', groups_path %>
